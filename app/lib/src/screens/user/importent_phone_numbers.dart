import 'package:flutter/material.dart';

class ImportantPhoneNumbers extends StatelessWidget {
  const ImportantPhoneNumbers({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
