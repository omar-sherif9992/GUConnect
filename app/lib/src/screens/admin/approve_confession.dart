import 'package:flutter/material.dart';

class ApproveConfessionScren extends StatelessWidget {
  const ApproveConfessionScren({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}